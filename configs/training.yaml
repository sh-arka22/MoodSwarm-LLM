parameters:
  finetuning_type: dpo
  num_train_epochs: 1         # DPO uses 1 epoch (hardcoded in finetune.py:353 anyway)
  per_device_train_batch_size: 2
  learning_rate: 2e-6         # DPO uses 150x lower LR than SFT (hardcoded in finetune.py:355 anyway)
  dataset_huggingface_workspace: saha2026
  is_dummy: false
